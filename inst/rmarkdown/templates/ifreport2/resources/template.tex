\documentclass[$if(classoptions)$$classoptions$$endif$]{ifreport2}

\usepackage{hyperref}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{float}
\usepackage{colortbl}
\usepackage{array}
\usepackage{multirow}
\usepackage[table]{xcolor}

$preamble$

$if(highlighting-macros)$
$highlighting-macros$
$endif$

\def\tightlist{}

\begin{document}

\title{$title$}
\author{$author$}

$if(date)$
\date{$date$}
$endif$

$if(emptydate)$
\date{}
$endif$

$if(name)$
\name{$name$}
$endif$

$if(leftlogo)$
\leftlogo{$leftlogo$}
$endif$

$if(rightlogo)$
\rightlogo{$rightlogo$}
$endif$

$if(tel)$
\tel{$tel$}
$endif$

$if(mail)$
\mail{$mail$}
$endif$

$if(saila)$
\saila{$saila$}
$endif$

$if(dpto)$
\dpto{$dpto$}
$endif$

$if(address)$
\addrs{$address$}
$endif$

\maketitle
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$


$body$

\end{document}
