\documentclass[$if(classoptions)$$classoptions$$endif$]{ifirak}

\usepackage{hyperref}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{float}
\usepackage{colortbl}
\usepackage{array}
\usepackage{multirow}
\usepackage[table]{xcolor}

$preamble$

$if(highlighting-macros)$
$highlighting-macros$
$endif$

\def\tightlist{}

\begin{document}

\title{$title$}
\author{$author$}

$if(date)$
$if($date$!="None"$
\date{$date$}
$endif$
$endif$

$if(center)$
\fakultatea{$center$}
$endif$

$if(teacher)$
\irakaslea{$teacher$}
$endif$

$if(year)$
\ikasturtea{$year$}
$endif$

$if(course)$
\irakasgaia{$course$}
$endif$

$if(tel)$
\tel{$tel$}
$endif$

$if(mail)$
\mail{$mail$}
$endif$

$if(address)$
\addrs{$address$}
$endif$

\maketitle
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$


$body$

\end{document}
