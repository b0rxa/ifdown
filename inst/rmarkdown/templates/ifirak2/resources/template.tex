\documentclass[$if(classoptions)$$classoptions$$endif$]{ifirak2}

\usepackage{hyperref}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{float}
\usepackage{colortbl}
\usepackage{array}
\usepackage{multirow}
\usepackage[table]{xcolor}

$preamble$

$for(header-includes)$
$header-includes$
$endfor$

$if(highlighting-macros)$
$highlighting-macros$
$endif$

\def\tightlist{}

\begin{document}

\title{$title$}
\author{$author$}

$if(date)$
\date{$date$}
$endif$

$if(emptydate)$
\date{}
$endif$

$if(name)$
\izena{$name$}
$endif$

$if(course)$
\irakasgaia{$course$}
$endif$

$if(tel)$
\tel{$tel$}
$endif$

$if(mail)$
\mail{$mail$}
$endif$

$if(address)$
\addrs{$address$}
$endif$

$if(dpto)$
\dpto{$dpto$}
$endif$

$if(saila)$
\saila{$saila$}
$endif$

\maketitle
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$


$body$

\end{document}
